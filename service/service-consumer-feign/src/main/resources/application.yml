eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-consumer-feign

#feign开启断路器
feign:
 hystrix:
  enabled: true